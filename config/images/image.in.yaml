apiVersion: kpack.io/v1alpha2
kind: Image
metadata:
  name: imgprocessor
spec:
  tag: ${DATA_E2E_REGISTRY_USERNAME}/ml-image-processing-app-image
  builder:
    kind: ClusterBuilder
    name: procfile
  source:
    git:
      url: https://github.com/agapebondservant/ml-image-processing-app.git
      revision: main
  cache:
    volume:
      size: 8G